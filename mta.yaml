_schema-version: "3.2"
ID: zrapya
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: zrapya-destination-content
  type: com.sap.application.content
  requires:
  - name: zrapya-destination-service
    parameters:
      content-target: true
  - name: zrapya-repo-host
    parameters:
      service-key:
        name: zrapya-repo-host-key
  - name: zrapya-uaa
    parameters:
      service-key:
        name: zrapya-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zrapya_html_repo_host
          ServiceInstanceName: zrapya-html5-service
          ServiceKeyName: zrapya-repo-host-key
          sap.cloud.service: zrapya
        - Authentication: OAuth2UserTokenExchange
          Name: zrapya_uaa
          ServiceInstanceName: zrapya-xsuaa-service
          ServiceKeyName: zrapya-uaa-key
          sap.cloud.service: zrapya
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: zrapya-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zrapya-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zrapya.zip
      name: zrapya
      target-path: resources/
- name: zrapya
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zrapya-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zrapya-destination-service
    service-plan: lite
- name: zrapya-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: zrapya-xsuaa-service
    service-plan: application
- name: zrapya-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zrapya-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
